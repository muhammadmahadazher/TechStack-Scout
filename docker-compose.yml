version: "3.8"

services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: techstack_scout
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build: .
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/techstack_scout
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - .:/app
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

  crawler:
    build: .
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/techstack_scout
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - .:/app
    command: python -m crawler.crawler
    # Run crawler on demand, or use a scheduler
    profiles: ["crawler"]

volumes:
  postgres_data:
